# Web Framework
fastapi==0.115.0
uvicorn==0.32.0
python-dotenv==1.0.1

# Microsoft Agent Framework (pre-release)
# uv handles pre-release versions automatically with much faster dependency resolution
agent-framework
